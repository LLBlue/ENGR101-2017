#
#Gitclones latest code, compiles, executes
#change the file to compile when we have one
#

git clone https://github.com/LLBlue/ENGR101-2017
cd ENGR101-2017/
gcc -o newest robot-methods.cpp
./newest
exit 0

#
#execute with "sh runLatest" while in directory
#
#if it doesn't work:
#chmod +x runLatest
#chmod 755 runlatest
#
